{
  "entities": {
    "JournalSession": {
      "title": "JournalSession",
      "description": "A journaling session created by an authenticated user",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier of the session"
        },
        "userId": {
          "type": "string",
          "description": "UID of the owning user"
        },
        "title": {
          "type": "string",
          "description": "Title or topic of the session",
          "maxLength": 200
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of session creation"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of last session update"
        },
        "moodTag": {
          "type": "string",
          "description": "Overall mood or sentiment detected for the session",
          "enum": ["reflective", "calm", "inspired", "anxious", "grateful", "neutral", "energized"]
        }
      },
      "required": ["userId", "title", "createdAt", "updatedAt"]
    },
    "JournalMessage": {
      "title": "JournalMessage",
      "description": "A message within a journaling session (user prompt or Gemini response)",
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique message ID"
        },
        "sessionId": {
          "type": "string",
          "description": "ID of the parent session"
        },
        "userId": {
          "type": "string",
          "description": "UID of the message author"
        },
        "role": {
          "type": "string",
          "description": "Role of the sender",
          "enum": ["user", "model"]
        },
        "text": {
          "type": "string",
          "description": "Content of the journal entry or reflection",
          "maxLength": 10000
        },
        "sentiment": {
          "type": "string",
          "description": "Sentiment score or emotion tag"
        },
        "createdAt": {
          "type": "string",
          "description": "ISO timestamp of message creation"
        }
      },
      "required": ["role", "text", "createdAt"]
    },
    "JournalSummary": {
      "title": "JournalSummary",
      "description": "AI-generated executive summary and themes of the session",
      "type": "object",
      "properties": {
        "summaryText": {
          "type": "string",
          "description": "Executive synthesis and reflection insights generated by Gemini",
          "maxLength": 5000
        },
        "keyThemes": {
          "type": "array",
          "description": "List of key themes or takeaways"
        },
        "updatedAt": {
          "type": "string",
          "description": "ISO timestamp of summary generation"
        }
      },
      "required": ["summaryText", "updatedAt"]
    }
  },
  "firestore": {
    "/users/{uid}/sessions/{sessionId}": {
      "schema": "JournalSession",
      "description": "User journal sessions scoped strictly under the owner's UID"
    },
    "/users/{uid}/sessions/{sessionId}/messages/{messageId}": {
      "schema": "JournalMessage",
      "description": "Messages and turns within a specific journal session"
    },
    "/users/{uid}/sessions/{sessionId}/summary": {
      "schema": "JournalSummary",
      "description": "System-generated summary doc restricted from direct client writes"
    }
  }
}
